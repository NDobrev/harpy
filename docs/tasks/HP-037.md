---
id: HP-037
status: done
depends_on: [HP-036]
acceptance: Tree-sitter Python extractor reproduces tests/test_symbols.py names; AST remains the fallback.
---

# HP-037 — Tree-sitter foundation and Python parity

Add `tree-sitter` plus the Python grammar. New `src/harpy/analysis/syntax/`
package. Bump `ANALYSIS_VERSION`.

`make check` must stay green.
