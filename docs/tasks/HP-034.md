---
id: HP-034
status: done
depends_on: [HP-033]
acceptance: apply_semantic runs one call per distinct model, merges annotations, degrades per-call, and scopes the cache key only when the plan is not the default.
---

# HP-034 — Multi-call semantic execution

Group-by-model pipeline, merge, and scope-aware cache keys.

`make check` must stay green.
