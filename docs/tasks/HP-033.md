---
id: HP-033
status: done
depends_on: [HP-032]
acceptance: compose_instruction emits only selected sections; default build_bundle stays compatible; per-call context filenames do not collide.
---

# HP-033 — Scoped prompt composition

Decompose the semantic instruction into per-scope fragments. Bump `PROMPT_VERSION`.

`make check` must stay green.
