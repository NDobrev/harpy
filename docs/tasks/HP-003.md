---
id: HP-003
status: done
depends_on: [HP-002]
acceptance: `harpy doctor --json` reports git, gh, auth, cursor-agent, rg, repo, resolved model.
---

# HP-003 — Add dependency doctor command

See docs/design/impl.md and AGENTS.md.

Acceptance: ``harpy doctor --json` reports git, gh, auth, cursor-agent, rg, repo, resolved model.`

`make check` must stay green.
