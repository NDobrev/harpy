---
id: HP-043
status: done
depends_on: [HP-042]
acceptance: Existing pipeline tests pass; cancellation kills process group; late events cannot replace another review; no subprocess outside proc.py.
---

# HP-043 — Extract the application service and cancellable execution

See docs/design/review-workspace.md §2.3 and §4 HP-043.
Extract acquisition, static analysis, semantic execution, and report assembly
behind ReviewService. Keep existing pipeline functions as compatibility adapters.
Add typed progress events. Remove shared mutation between semantic workers and
TUI results. Add cancellable process execution to proc.py.

`make check` must stay green.
