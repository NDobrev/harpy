---
id: HP-038
status: done
depends_on: [HP-037]
acceptance: TypeScript, Go, and Rust hunks populate changed_symbols with qualified names and visibility.
---

# HP-038 — TypeScript, Go, and Rust extractors

Dispatch by extension behind `SymbolExtractor`. Wire the pipeline so
`find_references` runs for these languages.

`make check` must stay green.
