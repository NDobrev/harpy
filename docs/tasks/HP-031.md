---
id: HP-031
status: done
depends_on: [HP-030]
acceptance: Navigable diagram pane cycles pictures, highlights hits, enter opens file; prompt emits structured sequence/flow/tree.
---

# HP-031 — Navigable diagram pane and prompt bump

`ApiDiagramView` highlights hits (`j`/`k`), cycles pictures (`[`/`]`), and `enter` opens
the matching file diff. Bump `PROMPT_VERSION` so the analyzer emits structured fields.

`make check` must stay green.
