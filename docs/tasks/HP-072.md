---
id: HP-072
status: done
depends_on: [HP-071]
acceptance: Legacy import, storage-backend marker, and TUI/SQL coexistence tests pass make check without weakening the TUI session suite.
---

# HP-072 — Legacy migration and local coexistence (WP-03)

Implement WP-03 from
[docs/design/web-review-workspace-prd.md](../design/web-review-workspace-prd.md).

- Import legacy JSON/catalog human work into tenant-aware SQL
- Atomic `storage-backend.json` marker after a verified import
- Compatibility adapter so TUI session save uses versioned decision/note writes
- Navigation save must not overwrite another client's shared state
- Fail closed on corrupt, newer, read-only, or changed sources; keep the original root

See [ADR 0017](../adr/0017-web-workspace-and-hosted-tenancy.md).
