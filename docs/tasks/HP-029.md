---
id: HP-029
status: done
depends_on: [HP-028]
acceptance: Leftover/changed FKs are labeled connectors; unchanged FKs stay omitted.
---

# HP-029 — ER leftover relations as connectors

Draw leftover (non-consecutive) and changed FK relations as labeled side connectors
between table boxes. Keep OLD/NEW split and ADR 0006 “only changed FKs”.

`make check` must stay green.
