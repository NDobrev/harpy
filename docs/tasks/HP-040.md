---
id: HP-040
status: done
depends_on: [HP-039]
acceptance: SYMBOL_FACTS.md is written once per semantic run; the inline bundle does not grow.
---

# HP-040 — SYMBOL_FACTS sidecar

Render facts in `semantic/`. Write from `apply_semantic` before call dispatch.
Scope-gate the pointer in `compose_instruction`. Bump `PROMPT_VERSION` to 11.

`make check` must stay green.
