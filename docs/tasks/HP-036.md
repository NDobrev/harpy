---
id: HP-036
status: done
depends_on: [HP-035]
acceptance: Two ALTER TABLE statements in one hunk do not cross-product columns onto the wrong table.
---

# HP-036 — Scope DDL column scans to one statement

`_objects_in` searched `_COLUMN` over the whole patch after each `ALTER TABLE`
match. Scope the column scan to the matched statement.

`make check` must stay green.
