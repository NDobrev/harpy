---
id: HP-027
status: done
depends_on: [HP-026]
acceptance: Diagram IR, mermaid subset parsers, RenderedDiagram hitboxes; schema_view wrappers leave existing tests green.
---

# HP-027 — Diagram IR and mermaid parsers

Structured sequence/flow/tree models, mermaid subset parsers, and `RenderedDiagram` hitboxes.
[`schema_view.py`](../../src/harpy/analysis/schema_view.py) remains a compatibility wrapper.

`make check` must stay green.
