---
id: HP-042
status: done
depends_on: []
acceptance: V2 records round-trip; invalid enums/ranges/ids rejected; architecture fixtures fail; existing tests unchanged.
---

# HP-042 — Establish the new contracts and architecture decisions

Add shared V2 records and enums from docs/design/review-workspace.md §2.
Preserve existing models. New reports use schema_version=2. Strict validation
on new provider response models; keep legacy parsing. ADRs for snapshot-based
reviews, durable review memory, staged analysis/cache identity, and unified TUI
navigation. Replace the fixed three-pane TUI rule with the responsive workspace
while preserving manual AI start. Architecture tests for storage, evidence,
review, verification, and TUI boundaries.

`make check` must stay green.
